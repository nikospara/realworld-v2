# This can be used to produce the env file utilized from docker-compose during Maven build.
# This file is for the container of the comments application.

QUARKUS_DATASOURCE_JDBC_URL=${database.comments.url}
QUARKUS_DATASOURCE_USERNAME=${database.comments.username}
QUARKUS_DATASOURCE_PASSWORD=${database.comments.password}

MP_MESSAGING_INCOMING_USERS_STREAM_BOOTSTRAP_SERVERS=${kafka.bootstrap.servers}
